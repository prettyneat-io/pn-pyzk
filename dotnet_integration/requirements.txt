# Python dependencies for PyZK .NET Integration
# Install with: pip install -r requirements.txt

# Core pyzk library
# If using from source, install the local package instead:
# pip install -e ..

# For production deployment, install from PyPI:
pyzk>=0.9.1
